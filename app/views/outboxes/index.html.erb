<%- model_class = Outbox -%>
<div class="page-header">
  <h1><%=t '.title', :default => model_class.model_name.human.pluralize.titleize %></h1>
</div>

<%= render 'outboxes', outboxes: @outboxes %>

 <br>
<%= link_to t('.new', :default => t("helpers.links.new")),
            new_outbox_path,
            :class => 'btn btn-primary' %>
