
$modal = $('#appModal'),
        $modalBody = $('#appModal .modal-body'),
        $modalHeading = $('#appModal .modal-header');

$modalHeading.html("Sorgente dati - [<%= @attachment.name %>]");

$modalBody.html("<%= j render('datasource', datasource: @attachment.datasource) %>");

$modal.modal();
