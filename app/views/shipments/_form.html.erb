<%= simple_form_for @shipment, :html => { :class => 'form-horizontal' } do |f| %>
  <%= f.input :description %>
  <%= f.input :template , as: :file %>
  <%= f.input :source,  as: :file %>
  <%= f.input :zf_id, collection: current_user.accounts,value_method: lambda { |obj| obj.channel.zf_id },label_method: lambda { |obj| (obj.channel.name + ':' + obj.connection_string) }, prompt: "Scegli un canale..." %>
    <% # f.input :zf_id, collection: current_user.accounts.channel,label_method: :name, value_method: :zf_id, prompt: "Scegli un canale..." %>
    <%# collection_select (:post,:zf_id, current_user.accounts, :zf_id, :connection_string,prompt: true) %>

  <div class="form-actions">
    <%= f.button :submit, :class => 'btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                shipments_path, :class => 'btn' %>
  </div>




  <% #debug( current_user.accounts ) %>


<% end %>
